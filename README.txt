To convert python script into build, follow the below steps:

STEP I: Install PyInstaller from PyPI:
pip install pyinstaller

STEP II: Go to your program’s directory and run:
pyinstaller <yourprogram>.py

This will generate the bundle in a subdirectory called dist.
for more details visit: https://pyinstaller.readthedocs.io/en/v3.3.1/
